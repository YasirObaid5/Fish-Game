# أعماق على أندرويد

افتح [اللعبة](https://brilliant-kataifi-874958.netlify.app/) في Chrome واضغط **تثبيت أعماق**. إذا لم يظهر طلب التثبيت، استخدم قائمة Chrome ثم **الإضافة إلى الشاشة الرئيسية → تثبيت**.

- تظهر اللعبة بأيقونتها الخاصة وتفتح كنافذة تطبيق مستقلة.
- تعمل العوالم الثلاثة والمؤثرات والصوت دون اتصال بعد ظهور «جاهزة للعب دون اتصال» في القائمة.
- اسحب دائرة السباحة بإصبع واضغط زر الاندفاع بإصبع آخر. الوضعان الطولي والأفقي مدعومان.
- أفضل نتيجة وإعدادات الصوت محفوظة على الجهاز؛ حذف بيانات الموقع يحذفها، وقد يزيل المتصفح الملفات المحفوظة عند نقص المساحة.
- التحديث ينزل عند الاتصال، ولا يقطع رحلة جارية أو متوقفة مؤقتاً. يمكن تطبيقه من القائمة أو بعد نهاية الجولة. إغلاق نوافذ اللعبة كلها يسمح أيضاً بتفعيل النسخة الجديدة عند الفتح التالي.
- هذه نسخة PWA، وليست ملف APK أو إصداراً منشوراً في Google Play.

## البناء والتحقق

`npm ci` ثم `npm run build` ينتج الموقع الكامل داخل `dist/`، بما فيه عامل الخدمة ذو نسخة مشتقة من محتوى الملفات. تُنشر محتويات هذا المجلد على HTTPS. لا تعتمد نسخة الهاتف على خادم صوت أو صور خارجي.

`npm test` لاختبارات قواعد اللعبة، و`npm run test:pwa` لاختبارات المتصفح وقطع الاتصال والتحديث الآمن. اختبارات `test:tides` و`test:audio` تغطي المؤثرات وقواعد اللعب والصوت. يمكن تعيين `CHROME_PATH` لمسار Chrome المثبت و`GAME_URL` لاختبار النسخة المنشورة.

الأيقونات PNG مُضمّنة في المستودع؛ لتجديدها بعد تعديل `icons/icon.svg`، شغّل `npm run icons` ثم أعد البناء. لا يحتاج بناء الإنتاج إلى تشغيل متصفح.

عامل الخدمة يخزّن نسخة كاملة بصورة ذرية، ويحدّث ملفاتها معاً. يتحقق من سلامة الملفات التنفيذية والأنماط والأيقونات قبل التفعيل. ملفات HTML مستثناة من مقارنة البصمة لأن الاستضافة قد تضيف بيانات وصفية. لا يطلب التطبيق إشعارات أو صلاحيات جهاز إضافية.
